#!/usr/bin/env bash
# Prepares the demo database: install gems, migrate, and seed demo data.
set -e
cd "$(dirname "$0")/.."
bundle check || bundle install
cd server
bundle check || bundle install
bundle exec rails db:prepare
bundle exec rails nquery:seed
