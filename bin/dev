#!/usr/bin/env bash
# Starts the demo server. Runs bin/setup first (migrate + seed) on every boot.
set -e
cd "$(dirname "$0")/.."
bin/setup
cd server
exec bundle exec rails server -b 0.0.0.0 -p "${PORT:-3000}"
